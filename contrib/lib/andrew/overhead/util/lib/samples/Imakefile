/* ********************************************************************** *\
 *         Copyright IBM Corporation 1988,1991 - All Rights Reserved      *
 *        For full copyright information see:'andrew/config/COPYRITE'     *
\* ********************************************************************** */
MkdirTarget($(DESTDIR)/lib)
MkdirTarget($(DESTDIR)/lib/samples)
InstallFile(README.ez, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.Xdefaults, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.atkinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.cshrc, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.ezinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.helpinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.messagesinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.preferences, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.sendmessageinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(bobg.typescriptinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.atkinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.bushinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.cshrc, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.ezinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.gdbinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.login, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.messagesinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.pipescriptinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(ghoti.preferences, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.Xclients, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.atkinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.cshrc, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.ezinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.login, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.messagesinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.preferences, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jhh.sendmessageinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.atkinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.consoleinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.cshrc, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.ezinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.gdbinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.messages-sendinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.pipescriptinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.preferences, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.sendmessageinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(jz1j.typescriptinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.atkinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.cshrc, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.ezinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.login, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.messagesinit, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
InstallFile(mcinerny.preferences, $(INSTINCFLAGS), ${DESTDIR}/lib/samples)
